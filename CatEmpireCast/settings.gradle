pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // JitPack: é de lá que vem a lib RootEncoder (RTMP + captura de
        // tela via MediaProjection) usada no BroadcastService.
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Cat Empire Cast"
include ':app'
